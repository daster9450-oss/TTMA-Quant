 # TTMA-Quant 專案開發守則

 ## 1. 溝通語言

 - 與使用者對話時一律使用繁體中文。
 - 程式碼註解、終端機輸出與錯誤訊息一律使用繁體中文；必要的技術名詞、套件名稱、指令與 API 名稱可保留原文。

 ## 2. 前端規範

 - 使用 React 開發前端。
 - 元件需符合 Framer Code Component 規範。
 - 不依賴外部 UI 函式庫；優先使用原生 React、HTML 與 CSS。
 - UI 一律以深色背景 `#0B0F17` 為基底，並使用青綠色 `#00F2FE` 作為強調色。

 ## 3. 後端規範

 - 使用 Python 開發後端。
 - 若執行時發現缺少套件，先以繁體中文提示缺少的套件，再執行 `pip install` 安裝必要依賴。

 ## 4. 時區對齊

 - 處理跨國金融資料時，所有日期與時間都必須明確標示時區並進行對齊。
 - 台股與日股等 T 日資料若對標美股收盤價，必須使用美股 T-1 日收盤價。
 - 不得僅以本機時區或未標示時區的日期直接合併金融資料；資料合併前需先確認交易日、收盤時間與時區轉換規則。
